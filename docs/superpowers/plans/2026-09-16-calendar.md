# 统一日历视图 Implementation Plan

**Goal:** 新增日历页面集中展示四类日期事件，本地与 CI 验证通过。
**Architecture:** 纯前端派生视图（calendar.ts 纯函数 + CalendarView），复用既有数据加载、导航、面板与配色体系；无后端改动。
**Spec:** docs/superpowers/specs/2026-09-16-calendar-design.md
**Constraints:** 事件语义与既有页面一致（费用外推不假设支付、月末 anchor、闰日钳 2/28）；账号数据仍由服务端会话隔离，本功能只读已下载数据。

- [x] 推导层：domain.ts 导出 iso/monthDays；新增 calendar.ts（monthGrid 周一起头、expenseOccurrences 周期外推、milestoneOccurrence 闰日钳制、buildCalendarEvents 逾期优先排序）。
- [x] 视图与集成：CalendarView.vue（月历、选中日面板、图例、跳转）；App.vue 导航与移动端 7 列；types.ts Page 增加 calendar；styles.css 桌面/暗色/移动端样式。
- [x] 测试：tests/calendar.test.ts 9 项纯函数用例；e2e 新增日历用例（桌面/手机），复用现有断言风格与时区固定。
- [x] 验证：Vitest 27 项、vue-tsc + build、Playwright 12 项、make test 全部通过；浏览器实测桌面与 390px 手机无横向溢出、标签收纳正确。
- [x] 文档：本计划与设计文档、README、HANDOFF 更新；提交 codex/calendar-view 分支。

## 执行结果

本地全部通过：Vitest 27 项（新增 9 项）、构建、Playwright 12 项（新增日历桌面/手机 2 项）、make test。浏览器验证了月历渲染、选日、翻月、「今天」复位、事件跳转与移动端布局。
