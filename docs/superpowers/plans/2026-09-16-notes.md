# 文字随记 Implementation Plan

**Goal:** 纯文本随记集合（迁移 0003），本地与 CI 验证通过。
**Architecture:** 复用 records.py 通用集合注册（第五个集合）；NotesView 独立视图 + ModalDialog 表单（随记无标题，与通用 RecordForm 结构不同）；导航扩为 8 项。
**Spec:** docs/superpowers/specs/2026-09-16-notes-design.md
**Constraints:** 账号隔离、多余键禁止、导出/导入闭环、CLI 恢复版本同步 0003、旧库迁移前仍先备份（deploy 流程不变）。

- [x] 后端：models.Note、migrations/0003、cli SCHEMA_REVISION=0003、schemas NotePayload/View/Patch、records COLLECTIONS 注册、imports 支持 notes；tests/test_notes.py 3 项（CRUD/隔离/导出导入、0002→0003 迁移保留）+ 既有断言随版本更新。
- [x] 前端：types Note、NotesView（分组、搜索、弹窗、删除确认）、App 接线（导航 8 项、加载/清理）、AppIcon notes 图标、移动端 8 列导航与卡片样式。
- [x] 修复：编辑弹窗曾把 id/created_at 混入表单导致 422，改为显式字段赋值（E2E 先复现）。
- [x] 验证：后端 110 项 pytest + Ruff、前端 30 项 Vitest + 构建、Playwright 18 项、make test；浏览器实测分组、无溢出、演示数据写入。
- [x] 文档：设计文档、本计划、api.md、README、HANDOFF；提交 codex/quick-notes 分支（叠加在 codex/json-import 上）。

## 执行结果

本地通过：后端 110 项（新增 3 项，迁移断言更新 1 项）、前端 30 项 Vitest、构建、Playwright 18 项（新增 2 项）。
