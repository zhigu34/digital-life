# 打卡 Implementation Plan

**Goal:** 每日必做/在做两类打卡（迁移 0005），本地与 CI 验证通过。
**Architecture:** 后端 app/checkins.py（项目 + 按日唯一日志，参照维护模块的归属/日期纪律）；前端 checkin.ts 纯函数 + CheckInsView 独立页面 + 今日页快捷面板；导出/导入闭环。
**Spec:** docs/superpowers/specs/2026-09-16-checkins-design.md
**Constraints:** 打卡日期按用户时区、不允许未来；同日唯一；数据归属由会话决定；连续天数为纯日历日计算。

- [x] 后端：models（CheckIn/CheckInLog + 同日唯一约束）、migrations/0005、cli 0005、checkins.py 全套路由（list 返回最近 400 天 days + total_count）、main/records/imports 接线；tests/test_checkins.py 4 项 + 既有版本断言更新。
- [x] 前端：types、checkin.ts（shiftDate/currentStreak/lastDays/countInRange/weekdayLabel/daysSinceLastCheck + 4 项单测）、CheckInsView（tabs、大圆打卡钮、7 天格子、管理弹窗补卡/删记录、编辑/归档/删除）、App 接线（导航 9 项、加载/清理）、TodayView「今天还没打卡」面板 + App 处理、AppIcon flame、桌面/移动样式。
- [x] 修复：管理弹窗底部按钮与头部关闭重名导致 E2E 严格模式冲突，改名「完成」。
- [x] 验证：后端 125 项 pytest + Ruff、前端 34 项 Vitest + 构建、Playwright 22 项、make test；浏览器实测桌面卡片（连续/格子/撤销态）与 390px 手机 9 列导航无溢出。
- [x] 文档：设计文档、本计划、api.md、README、HANDOFF；提交 codex/checkins 分支。

## 执行结果

本地通过：后端 125 项（新增 4 项）、前端 34 项（新增 4 项）、E2E 22 项（新增 2 项）。
